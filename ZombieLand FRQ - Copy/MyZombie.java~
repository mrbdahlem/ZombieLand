import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Help Karl collect all of the brains.
 * 
 * @author Neil Naik 
 * @version 8/24/17
 */
public class MyZombie extends UltraZombie
{
    /**
     * Karl's planned out actions to find the longest line of brains in front of him.
     */
    public void plan()
    {
        nextBrain();//Brings Karl to the first set of brains.
        followBrains();//Climbs the first column of brains.
        while(isBrainHere()){//Starts the brain sorting sequence.
            turnLeft();
            move();
            nextBrain();//Karl looks for a higher column of brains.
            if(!isBrainHere()){//Brings Karl back to original column he left.
                turnAround();
                nextBrain();
                turnLeft();
                win();
            }else{//Moves Karl to the top of the new column.
                followBrains();
            }
        }
        win();//So much time wasted for 1 point.
    }
    public void nextBrain(){//Finds the next row of brains.
        while(!isBrainHere()&&isFrontClear()){
            move();
        }
    }
    public void followBrains(){//Instructs Karl to walk to the top of the column of brains
        turnLeft();
        while(isBrainHere()&&isFrontClear()){//Karl moves through all brains and avoids walls.
            move();
        }
        turnAround();
        if(!isBrainHere()){//Brings Karl back to the stack if he isn't stopped by a wall. Also the only part I missed, and I did all of this for that one point.
            move();
        }
    }
}